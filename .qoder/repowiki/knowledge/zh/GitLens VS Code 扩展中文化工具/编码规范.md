- JSON 序列化时统一使用 `ensure_ascii=False` 并配合制表符缩进（`indent='\t'`），以保持与 VS Code 插件元数据格式的一致性。
- 采用递归遍历（`extract_translations` / `apply_translations`）处理深层嵌套的 JSON 结构，并通过路径字符串（如 `contributes.commands[0].title`）精确定位翻译键值。
- 通过正则表达式 `[一-鿿]` 检测字符串是否包含中文字符，以此作为判断翻译状态或提取未翻译内容的核心依据。